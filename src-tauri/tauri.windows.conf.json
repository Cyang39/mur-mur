{
  "$schema": "https://schema.tauri.app/config/2",
  "build": {
    "beforeBuildCommand": "powershell -NoProfile -Command \"$env:NEXT_DISABLE_SWC_NATIVE='1'; pnpm build\"",
    "frontendDist": "../dist"
  },
  "bundle": {
    "externalBin": [
      "bin/whisper-cli",
      "bin/whisper-cli-vulkan",
      "bin/ffmpeg"
    ]
  }
}
